# GitHub Pagesへのデプロイ手順

> ⚠️ **免責事項**: このドキュメントは個人によって作成されたガイドです。手順の正確性や完全性を保証するものではありません。GitHubの仕様変更により、手順が変わる可能性があります。

このドキュメントでは、IBM Bob LinksサイトをGitHub Pagesで公開する手順を説明します。

## 📋 前提条件

- GitHubアカウントを持っていること
- Gitがインストールされていること
- リポジトリへの書き込み権限があること
- 基本的なGitコマンドの知識があること

**注**: 問題が発生した場合は、[TROUBLESHOOTING.md](TROUBLESHOOTING.md)を参照してください。サポートはベストエフォートベースで提供されます。

## 🚀 初回デプロイ手順

### 1. GitHubリポジトリの作成

既にリポジトリがある場合はこのステップをスキップしてください。

```bash
# GitHubで新しいリポジトリを作成後、ローカルで初期化
cd /Users/ayatokura/IBMBob
git init
git add .
git commit -m "Initial commit: IBM Bob Links site"

# リモートリポジトリを追加
git remote add origin https://github.com/ayatokura/ibmbob-links.git
git branch -M main
git push -u origin main
```

### 2. GitHub Pagesの有効化

1. GitHubリポジトリページ（https://github.com/ayatokura/ibmbob-links）を開く
2. **Settings**（設定）タブをクリック
3. 左サイドバーの**Pages**をクリック
4. **Source**セクションで以下を選択：
   - **Branch**: `main`
   - **Folder**: `/ (root)`
5. **Save**をクリック

### 3. デプロイの確認

数分後（通常1-5分）、以下のURLでサイトが公開されます：

```
https://ayatokura.github.io/ibmbob-links/
```

デプロイ状況は以下で確認できます：
- リポジトリの**Actions**タブ
- **Settings** > **Pages**の上部に表示される公開URL

## 🔄 更新手順

ファイルを変更した後、以下のコマンドで更新をデプロイします：

```bash
# 変更をステージング
git add .

# コミット（変更内容を説明）
git commit -m "Update: 変更内容の説明"

# GitHubにプッシュ
git push origin main
```

数分後に自動的にサイトが更新されます。

## 📁 プロジェクト構造

GitHub Pagesで正しく動作するために、以下の構造を維持してください：

```
IBMBob/
├── index.html              # エントリーポイント（必須）
├── .nojekyll              # Jekyllビルドを無効化（重要）
├── css/
│   └── articles.css
├── js/
│   ├── qiita-api.js
│   ├── article-manager.js
│   └── ui-controller.js
├── VibeCodingImage.jpg
├── README.md
├── DEPLOY.md              # このファイル
└── LICENSE
```

## ⚙️ 重要な設定ファイル

### .nojekyll

このファイルは、GitHub PagesでJekyllビルドを無効化するために必要です。
空のファイルで、アンダースコアで始まるディレクトリ（例：`_private`）も正しく配信されます。

## 🔧 トラブルシューティング

### サイトが表示されない

1. **デプロイ状況を確認**
   - リポジトリの**Actions**タブでビルド状況を確認
   - エラーがある場合は詳細を確認

2. **ブランチとフォルダを確認**
   - **Settings** > **Pages**で正しいブランチ（`main`）とフォルダ（`/`）が選択されているか確認

3. **キャッシュをクリア**
   - ブラウザのキャッシュをクリアして再読み込み
   - シークレットモードで開いて確認

### 404エラーが表示される

1. **index.htmlの存在を確認**
   ```bash
   ls -la index.html
   ```

2. **ファイル名の大文字小文字を確認**
   - GitHub Pagesは大文字小文字を区別します
   - すべてのファイル名とパスが正確か確認

### CSSやJavaScriptが読み込まれない

1. **相対パスを確認**
   - すべてのパスが相対パス（`./`または`../`）になっているか確認
   - 絶対パス（`/`で始まる）は避ける

2. **ファイルの存在を確認**
   ```bash
   ls -la css/articles.css
   ls -la js/*.js
   ```

### Qiita APIが動作しない

1. **ブラウザのコンソールを確認**
   - F12キーで開発者ツールを開く
   - Consoleタブでエラーメッセージを確認

2. **CORS設定を確認**
   - Qiita APIは公開APIでCORS対応済み
   - 追加の設定は不要

3. **キャッシュをクリア**
   ```javascript
   // ブラウザのコンソールで実行
   localStorage.removeItem('ibmbob_articles_cache');
   location.reload();
   ```

## 🔐 セキュリティ

### HTTPS

GitHub Pagesは自動的にHTTPSを有効にします。
**Settings** > **Pages**で「Enforce HTTPS」が有効になっていることを確認してください。

### APIキー

このプロジェクトはQiita APIの公開エンドポイントのみを使用し、APIキーは不要です。
認証情報をコミットしないよう注意してください。

## 📊 制限事項

GitHub Pagesの制限：
- **リポジトリサイズ**: 1GB推奨
- **ファイルサイズ**: 100MB以下
- **帯域幅**: 月間100GB
- **ビルド回数**: 1時間あたり10回

通常の使用では問題ありません。

## 🌐 カスタムドメイン（オプション）

独自ドメインを使用する場合：

1. **Settings** > **Pages** > **Custom domain**にドメインを入力
2. DNSプロバイダーでCNAMEレコードを設定：
   ```
   www.yourdomain.com -> ayatokura.github.io
   ```
3. **Enforce HTTPS**を有効化

詳細は[GitHub Docsのカスタムドメインガイド](https://docs.github.com/ja/pages/configuring-a-custom-domain-for-your-github-pages-site)を参照してください。

## 📚 参考リンク

- [GitHub Pages ドキュメント](https://docs.github.com/ja/pages)
- [GitHub Pages のクイックスタート](https://docs.github.com/ja/pages/quickstart)
- [GitHub Pages のトラブルシューティング](https://docs.github.com/ja/pages/getting-started-with-github-pages/troubleshooting-404-errors-for-github-pages-sites)

## 💡 ヒント

### 開発時のローカルサーバー

ローカルで開発する際は、簡易HTTPサーバーを使用：

```bash
# Python 3の場合
python3 -m http.server 8000

# Node.jsの場合（http-serverをインストール後）
npx http-server -p 8000
```

ブラウザで `http://localhost:8000` を開いて確認できます。

### デプロイ前のチェックリスト

- [ ] すべてのファイルがコミットされている
- [ ] 相対パスが正しく設定されている
- [ ] .nojekyllファイルが存在する
- [ ] index.htmlがルートディレクトリにある
- [ ] ローカルで動作確認済み

## 🆘 サポート

問題が解決しない場合：

1. [GitHubのIssue](https://github.com/ayatokura/ibmbob-links/issues)を作成
2. エラーメッセージとスクリーンショットを添付
3. 実行した手順を詳しく記載

---

Made with ❤️ for the IBM Bob community